<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tita">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".utils.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tita"
        android:usesCleartextTraffic="true">

        <activity
            android:theme="@style/Theme.Tita.NoActionBar"
            android:label="board"
            android:name=".ui.board.BoardActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".ui.login.LoginActivity">
        </activity>

        <activity
            android:name=".ui.findidpassword.activity.FindPasswordActivity"
            android:label="@string/title_activity_find_password"
            android:theme="@style/Theme.Tita.NoActionBar">

        </activity>
        <activity android:name=".ui.findidpassword.activity.FindIdActivity" />
    </application>

</manifest>